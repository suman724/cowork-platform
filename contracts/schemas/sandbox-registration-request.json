{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cowork.dev/schemas/sandbox-registration-request.json",
  "title": "SandboxRegistrationRequest",
  "description": "Request sent by a sandbox container to register with the Session Service after startup.",
  "type": "object",
  "required": ["sandboxEndpoint", "taskArn"],
  "properties": {
    "sandboxEndpoint": {
      "type": "string",
      "description": "Internal IP:port where the sandbox is listening (e.g., http://10.0.1.42:8080)."
    },
    "taskArn": {
      "type": "string",
      "description": "ECS task ARN read from the task metadata endpoint. Must match the expectedTaskArn stored at RunTask time."
    }
  },
  "additionalProperties": false
}
